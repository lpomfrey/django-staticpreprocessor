language: python
python:
    - "2.7"
install:
    - "pip install -r requirements.txt --use-mirrors"
    - "pip install mock"
before_script:
    - "pip install python-coveralls coverage"
script:
    - "coverage run manage.py test staticpreprocessor"
after_success:
    - "coveralls"
notifications:
    email: false
